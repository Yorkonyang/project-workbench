# 升级说明 | 权限模型重构（项目负责制）

> 适用版本：项目工作台 v2（含 2026-08-20 权限改造）
> 变更性质：**破坏性权限重构** —— 旧版 RBAC 权限矩阵已作废

---

## 一、变更背景

原权限方案以"角色 + 权限矩阵"为核心，但实际使用中暴露问题：以「项目成员」角色登录、且未参与任何项目的用户，仍然能在仪表盘看到**全部项目**、拥有与管理员相同的项目列表权限，并可新建项目——与权限预期严重不符。

因此废弃旧权限矩阵，全面改为 **项目负责制（Project Responsibility Model）**。

---

## 二、新权限模型

| 角色 | 判定条件 | 能力范围 |
|---|---|---|
| 所有人 | — | 可创建项目 / 任务 / 待办（平台人人可用） |
| 所有者 Owner | `project.ownerId === 当前用户` **或** `project.manager === 当前用户` | 查 / 建 / 改 / 删**自己负责的项目**及其任务、待办、文档、里程碑；**看不到他人项目** |
| 成员 Member | 被分配到某任务（`task.assignees` 数组或 `task.assignee` 含当前用户） | 可在所属任务范围内**上报进度**、追加待办 / 文档；**不能改 / 删项目与任务** |
| 管理员 Admin | `member.role === 'admin'` | 最高权限，可见并管理全部数据 |

> 项目负责人（`manager`）是**指定字段**，与"谁点击新建"分离：部门负责人可为部门人员**安排**项目，被安排者成为该项目的成员。

---

## 三、后端改造

文件：`server/accessControl.js`（新增）、`server/simple-server.js`

- **集中权限判定**：`getUserId`（优先 `x-user-id` 请求头，次 URL query）、`isAdmin`、`isProjectOwner`、`canManageProject`、`canManageTask`、`canCreateTask`、`canCreateTodo`、`canCreateDocument`、`canManageTodo`、`canManageDocument`、`canManageMilestone`、`visibleProjects/Tasks/Todos/Documents/Milestones` 等。
- **真实隔离**：
  - GET `/api/projects`、`/api/tasks`、`/api/todos`、`/api/milestones`、`/api/documents` 均按 `userId` 过滤（成员只看被分配内容）。
  - 项目详情、项目 / 任务的 PUT / DELETE、里程碑 / 文档的 POST / PUT / DELETE 均加 **403** 越权校验。
- **创建即归属**：新建项目自动 `ownerId = 当前用户`。
- **成员进度汇报放行**：PUT 任务允许所有者**或**任务责任人（成员上报进度）。
- **历史数据迁移**：为已有项目补 `ownerId`（兜底为 `manager` / admin），保证旧数据可被正确归属。

---

## 四、前端改造

- `src/lib/apiClient.js`：每个请求自动附加 `x-user-id` 请求头。
- `src/hooks/useAccess.js`：前端权限判定（`isAdmin`、`canManageProject`、`canManageTask`、`canReportTask`、`canManageTodo`、`canManageDocument`、`canManageMilestone`）。
- 越权按钮隔离：`TaskCard`、`TaskList`（编辑 / 删除包 `canEdit`，进度包 `canReport`）、`ProjectCard`、`ProjectDetailPage / TasksPage / TimelinePage / DocumentsPage` 的"新建"按钮按角色隐藏；`TodoForm / DocumentForm` 的项目下拉仅显示可关联项目。
- `src/lib/bootstrap.js`（新增）：登录 / 登出后**清空数据 store 的 localStorage 缓存并重置内存**，再以当前用户重新拉取，防止跨账号数据残留。
- `src/App.jsx`：启动时仅清数据缓存（保留 `pw_auth`），未登录不预拉数据，并在登录态变化时兜底刷新。

### 本轮修复的 3 个回归问题（同一轮改造引入）

1. **Zustand persist 缓存残留**：数据 store 持久化到 localStorage，切换账号后旧数据继续展示 → 已用 `bootstrap.js` 在登录 / 登出时清空重拉。
2. **成员名册未加载**：旧清理逻辑误删 `pw_members` 缓存且无加载路径，导致登录比对空数组报"邮箱或密码错误" → 改为 mount 时无条件 `fetchMembers()`。
3. **CORS 拦截 `x-user-id`**：后端 CORS 头未声明该自定义头，预检失败使所有 `/api/*` 请求被浏览器拦截 → 已在 `sendResponse()` 的 `Access-Control-Allow-Headers` 加入 `x-user-id`。

---

## 五、已删除内容

- `src/components/members/PermissionMatrix.jsx`（旧 RBAC 权限矩阵组件）及 `MembersPage.jsx` 中的渲染与引用。
- 人员管理下的旧权限矩阵 UI 不再存在；admin 角色筛选与管理按钮仍保留（项目负责制下 admin 为最高权限）。

---

## 六、升级 / 部署步骤

```bash
# 1. 拉取最新代码
git pull

# 2. 安装依赖
npm install

# 3. 启动后端（端口 3000）
PORT=3000 DB_PATH=data/workbench.db node server/simple-server.js

# 4. 启动前端（端口 5173）
npm run dev
```

### 历史数据处理
- 若从旧版本升级，首次启动后端会对已有项目**自动补 `ownerId`**（兜底 manager / admin），无需手动迁移。
- 前端请在升级后 **Ctrl+Shift+R 硬刷新** 浏览器，清除 localStorage 中旧版本的缓存数据，避免旧项目列表残留。

---

## 七、数据库导出 / 导入

数据管理（Header 齿轮图标）支持导出备份（`workbench.db` 快照）与导入恢复。生产环境建议定期备份 `data/workbench.db`。

---

## 八、注意事项

- `data/` 目录已被 `.gitignore` 排除，**请勿将运行时数据库提交到仓库**（含真实成员邮箱与明文密码）。
- 早期 Git 提交中可能仍包含历史版本的 `data/workbench.db`。如需彻底清除，请使用 `git filter-repo` 重写历史后强推（破坏性，谨慎操作）。
- 默认鉴权适用于内网 / 受信任环境；公网部署务必增加 HTTPS 与更强身份认证。
