# 信息化中心管理平台 - QA 验证报告

> **日期**：2026-08-16  
> **方式**：主理人直接执行后端冒烟 + 全链路 API 验证 + 前端构建验证 + 生产模式验证  
> **结论**：✅ 全部通过，MVP 可交付

---

## 一、验证环境

| 项目 | 值 |
|------|-----|
| Node.js | v22.22.2（managed） |
| 数据库 | SQLite（better-sqlite3 v13.0.0，预编译二进制） |
| 后端 | Express 4，端口 3001 |
| 前端 | Vite 5 开发服务器（5174/5175）+ 生产构建 dist |
| 操作系统 | Windows 10 (64-bit) |

---

## 二、测试结果明细

### 2.1 后端启动（✅ 通过）

| 检查项 | 结果 |
|--------|------|
| 数据库自动建表（7张核心表） | ✅ 成功 |
| 种子数据写入（4用户 + 5系统 + 默认设置） | ✅ admin/admin123 |
| 探活定时任务启动（每分钟） | ✅ 正常 |
| 服务监听 3001 | ✅ 正常 |

### 2.2 探活引擎联动（✅ 通过）

| 场景 | 结果 |
|------|------|
| 示例 qms 系统（无效URL）被探测为宕机 | ✅ "dow x1: 网络错误: fetch failed" |
| 宕机自动创建 critical 告警 | ✅ alerts 表 +1 |
| 告警自动生成工单（source=alert） | ✅ tickets 表 +1 |
| 轻流未配置时安全跳过推送 | ✅ "未配置轻流，跳过告警推送" |
| 恢复检测逻辑（ok状态自动关闭旧告警） | ✅ 代码审查确认 |

### 2.3 认证与用户（✅ 通过）

| 场景 | 结果 |
|------|------|
| admin/admin123 登录 | ✅ 返回 JWT token |
| 未带 token 访问受保护接口 | ✅ 401 拒绝 |
| 用户列表（分派下拉） | ✅ 4 个用户 |

### 2.4 工单全生命周期（✅ 通过）

| 场景 | 结果 |
|------|------|
| 创建工单（含 SLA 期限自动计算） | ✅ TKT-20260816-002 |
| 分派处理人 | ✅ 状态 open→assigned |
| 流转至处理中（记录响应时间） | ✅ response_at 写入 |
| 流转至已解决（记录解决时间） | ✅ resolved_at 写入 |
| 操作记录审计 | ✅ ticket_actions 完整 |
| 工单统计 / 团队工作量 | ✅ 正常工作 |
| 非法状态流转拒绝 | ✅ VALID_TRANSITIONS 校验 |

### 2.5 仪表盘聚合（✅ 通过）

| 检查项 | 结果 |
|--------|------|
| 系统状态汇总（ok=4, down=1） | ✅ 正确 |
| 我的工单 / 今日新增 / 逾期数 | ✅ 正确返回 |
| 未读通知计数 | ✅ 正确 |
| 团队工作量（admin 专属） | ✅ 4 行数据 |

### 2.6 生产模式（✅ 通过）

| 检查项 | 结果 |
|--------|------|
| 首页 HTML 托管 | ✅ 返回 index.html |
| 静态资源 (JS/CSS) | ✅ HTTP 200 |
| SPA 路由回退（/systems 直接访问） | ✅ 返回 index.html |
| API 与静态托管共存 | ✅ 不冲突 |

### 2.7 前端构建（✅ 通过）

| 检查项 | 结果 |
|--------|------|
| Vite 生产构建 | ✅ 1010 模块转换，4.49s |
| 构建产物 | ✅ dist/index.html + assets |

---

## 三、已知问题与处置

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | better-sqlite3 v9 无 Node22 预编译 | ✅ 已修复 | 升级至 v13.0.0（package.json 已更新） |
| 2 | 前端 chunk 543KB > 500KB 警告 | ⚠️ 非阻塞 | 仅性能提示，后续可用动态导入优化 |
| 3 | 示例 qms 系统 URL 无效导致每次启动报宕机 | ✅ 预期行为 | 上线时替换为真实系统地址即可 |
| 4 | Vite 5174 端口偶发占用 | ⚠️ 环境问题 | 自动切换 5175，无功能影响 |

---

## 四、QA 智能路由判定

**判定：NO_ONE（无缺陷路由）** — 后端无源码 Bug，测试全部通过。

- 源码 Bug：无 → 无需回退工程师
- 测试代码 Bug：无 → 无需 QA 自修
- 遗留问题：仅性能优化建议（chunk 拆分），不影响 MVP 使用

---

## 五、交付物清单

**需求/设计文档**
- `docs/itc-platform-prd.md`（已确认）
- `docs/itc-platform-architecture.md`（已确认）

**应用代码（itc-platform/）**
- 后端 25 个 JS 文件（Express + better-sqlite3 + node-cron）
- 前端 13 个源文件（React + Vite + MUI + Tailwind）
- 部署：docker-compose.yml + Dockerfile
- 说明：README.md

**启动方式（开发）**
```bash
cd server && npm install && npm start    # 后端 :3001
cd web && npm install && npm run dev     # 前端 :5174
# 访问 http://localhost:5174 ，admin/admin123
```

**启动方式（生产）**
```bash
docker compose up -d --build             # 访问 http://<IP>:3001
```